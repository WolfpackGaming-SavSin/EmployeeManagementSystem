<tr>
    <td><%= program.name %></td>
    <td><%= program.description %></td>
    <td>
        <%= link_to "View", program_path(program.id) %> 
        <% if current_user.admin? %>
            | 
            <%= link_to "Delete", program_path(program.id), method: :delete, data: { confirm: "Are you sure you want to delte this application?"} %>
        <% end %>
    </td>
</tr>